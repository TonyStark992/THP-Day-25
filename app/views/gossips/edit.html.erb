<h1>Editer le potin</h1></br>

<p>
    <% if @gossip != nil%>
        <% if @gossip.errors.any? %>
        <p>Nous n'avons pas réussi à éditer le potin pour la (ou les) raison(s) suivante(s) :</p>
        <ul>
            <% @gossip.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
        <% end %>
    <% end %>
</p>

<%= form_for @gossip do |f| %>
  <div class="form-group">
    <label>Titre du potin</label>
    <%= f.text_field :title, class: 'form-control' %>
  </div>
  <div class="form-group">
    <label>Le potin</label>
    <%= f.text_area :content, class: 'form-control' %>
  </div>
  <div class="form-group">
    <button type="submit" class="btn btn-primary">Modifier le potin</button>
  </div>

<% end %>