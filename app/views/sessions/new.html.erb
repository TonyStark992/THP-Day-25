<h5>Remplissez le formulaire ci-dessous pour vous inscrire sur The Gossip Project !</h5>

<p>
    <% if @user != nil%>
        <% if @user.errors.any? %>
        <p>Nous n'avons pas réussi à vous connecter à votre compte pour la (ou les) raison(s) suivante(s) :</p>
        <ul>
            <% @user.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
        <% end %>
    <% end %>
</p>

<p>
      <%= form_tag url_for(action: 'create'), method: "post" do %>

        <%= label_tag 'Email' %> 
        <%= text_field_tag 'email' %>
        <br /> <br /> 

         <%= label_tag 'Mot de passe' %> 
        <%= text_field_tag 'password' %>
        <br /> <br />

        <%= submit_tag "Se connecter" %> 

      <% end %>
</p>